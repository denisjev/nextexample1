import NotFound from "./not-found";

export default function PersonsLayout({ children })
{
    return(
        <div>
            <h1>Marco de trabajo</h1>
                {children} 
        </div>
    )
}